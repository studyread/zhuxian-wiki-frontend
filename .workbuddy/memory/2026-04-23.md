# 2026-04-23 工作日志

## 管理后台路由修复
- `admin-router.js` 路由路径全部去掉 `/admin` 前缀（如 `/admin/login` → `/login`），因为 base 已经是 `/admin.html`
- `AdminLayout.vue` 菜单路径同步修正，登出改用 `window.location.href = '/admin.html/login'`
- `WikiHeader.vue` 后台管理按钮改为 `window.location.href = '/admin.html/login'`（前台和后台是独立 app，不能用 router.push）
- `AdminLogin.vue` 登录成功后改用 `router.push('/dashboard')`（history 模式，不用 hash）

## 分类路由类型错误修复
- 导航栏 `/category/攻略` 传中文名给后端 `@PathVariable Long id` 导致类型错误
- 后端 `CategoryController` 新增 `/api/categories/by-name/{name}` 接口按名称查分类
- 前端 `article.js` 的 `categoryApi` 添加 `getByName` 方法
- 前端 `Category.vue` 的 `loadCategory` 和 `loadArticles` 自动判断参数是数字 ID 还是中文名

## Redis 连接失败降级
- `ArticleService` 和 `UserService` 所有 Redis 操作加 try-catch
- Redis 挂了时降级走 DB，不再抛出 500 异常

---

# 2026-04-25 工作日志

## 管理后台深色主题重构
- `AdminLayout.vue` 完全重写为深色主题（#1a1a2e 深蓝背景）
- 统一侧边栏、顶部导航、内容区域样式
- 新增搜索框、用户头像等元素
- `AdminDashboard.vue` 重构为深色主题，包含统计卡片、分类分布图、最近操作
- `AdminPage.vue` 简化为纯 wrapper（只用于保留 slot）
- 所有子页面移除 AdminPage wrapper，统一由 AdminLayout 提供布局
- 路由重构：AdminLayout 作为父路由，子路由嵌套在其 children 下

## 管理员登录功能
- 密码问题修复：`ZhuxianWikiApplication` 启动时强制重置默认管理员密码
- 登录账户：`admin` / `admin123`

## Git 存档
- 初始存档 commit: `Initial archive before admin improvements`
- 深色主题 commit: `完善管理后台界面 - 深色主题重构 + 管理员登录功能`
- 如需回档：`git log` 查看 commit 历史，`git checkout <commit-hash>` 回退
