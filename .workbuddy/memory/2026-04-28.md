# 2026-04-28 工作记录

## 管理员登录500错误修复
- 根本原因：Admin实体字段 `lastLoginAt` 与数据库列 `last_login` 不匹配
- 修复方案：执行 `database/fix_admin_column.sql`，将 `last_login` 重命名为 `last_login_at`
- 同时修复了 `ZhuxianWikiApplication.java` 强制重置密码的逻辑（去掉了每次启动都重置密码的代码）
- AdminService 支持明文/BCrypt双模式密码验证

## 图片上传容量提升至5MB
- **ArticleEdit.vue（写攻略封面）**：去掉了Base64转码后仅允许2MB的二次限制，改为7MB（对应原图约5MB）
- **KnowledgeEdit.vue（知识库图片）**：已是5MB限制，无需修改
- **后端 application.yml**：新增 multipart 配置（max-file-size: 10MB, max-request-size: 20MB）
- MySQL连接URL已含 maxAllowedPacket=16777216（16MB），足够支持Base64图片提交
