<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhuxianwiki.mapper.UserArticleLikeMapper">
    
    <select id="selectCountByUserAndArticle" resultType="int">
        SELECT COUNT(*) FROM user_article_like 
        WHERE user_id = #{userId} AND article_id = #{articleId}
    </select>
    
    <delete id="deleteByUserAndArticle">
        DELETE FROM user_article_like 
        WHERE user_id = #{userId} AND article_id = #{articleId}
    </delete>
    
</mapper>
